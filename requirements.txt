# Core Django
Django==5.0.6

# Database + ORM helpers
sqlparse==0.5.0

# Excel & data handling
pandas==2.2.2
openpyxl==3.1.2

# Timezone and date utilities
pytz==2024.1
Babel==2.15.0

# Web server and HTTP utilities
asgiref==3.8.1

# Authentication & password hashing
argon2-cffi==23.1.0

# For serving static files easily in dev (optional but useful)
whitenoise==6.6.0

# For chart rendering (frontend via CDN, not needed here, but kept for clarity)
# chart.js is loaded from CDN in templates, no need to install it.

# If you use Django REST or plan future APIs (optional)
# djangorestframework==3.15.2
